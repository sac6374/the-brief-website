{
  "permissions": {
    "allow": [
      "Bash(python3 -m py_compile scripts/generate_brief.py)",
      "Bash(python3 -c \"from datetime import date; print\\(date.today\\(\\).strftime\\('%A, %B %-d, %Y'\\)\\)\")"
    ]
  }
}
